# Architecture

This document defines the architecture of the Material Impact Agent for
Target Value Design (TVD).

The system is intentionally designed as a single-agent architecture
with deterministic computation and auditable iteration memory.
